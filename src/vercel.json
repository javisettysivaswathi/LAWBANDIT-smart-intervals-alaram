{
  "version": 2,
  "builds": [
    { "src": "src/index.ts", "use": "@vercel/node" },
    { "src": "FRONTEND/**/*", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "src/index.ts" },
    { "src": "/(.*)", "dest": "/FRONTEND/$1" }
  ]
}
